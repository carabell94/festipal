document.querySelector('.display-day').innerHTML = "";
document.querySelector('.display-day').insertAdjacentHTML('beforeend', `<%= render 'festivals/partials/display-sunday', festival: @festival %>`);
