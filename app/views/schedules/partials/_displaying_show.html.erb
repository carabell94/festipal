  <% if concert.schedule.stage.festival.name == @festival.name %>
    <% if concert.schedule.end_time == 24 %>
      <p> <%= link_to schedule_user_schedule_path(concert), method: :delete, remote: true do %>
          <i class="fas fa-minus-square"></i>
          <% end %>
          <%= concert.schedule.artist %> - <%= concert.schedule.day %> <%= concert.schedule.stage.name %> <%= concert.schedule.start_time %>.00 - 00.00</p>
    <% else %>
      <p> <%= link_to schedule_user_schedule_path(concert), method: :delete, remote: true do %>
          <i class="fas fa-minus-square"></i>
          <% end %>
          <%= concert.schedule.artist %> - <%= concert.schedule.day %> <%= concert.schedule.stage.name %> <%= concert.schedule.start_time %>.00 - <%= concert.schedule.end_time %>.00</p>
    <% end %>
  <% end %>

